package no.api.meteo.jaxb;

import org.junit.Assert;
import org.junit.Test;

public class ExtremeForecastTest extends AbstractTest {

    @Test
    public void test1_0() {
        // No test data available from MET + We only have a DTD not a XMLSchema
        Assert.assertEquals("", "");
    }
}
